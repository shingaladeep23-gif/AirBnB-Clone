/**
 * Global footer: link columns plus the bottom legal/currency bar.
 * STUB — T4.
 */
export function SiteFooter() {
  return (
    <footer className="border-t border-border-subtle bg-surface-sunken">
      <div className="mx-auto w-full max-w-content px-10 py-12">
        {/* T4: link columns + legal bar. */}
      </div>
    </footer>
  );
}
