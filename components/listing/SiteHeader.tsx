/**
 * Sticky top navigation: logo, collapsed search pill, host/globe/profile cluster.
 * STUB — T4. Height is tokenised (`--spacing-header-h`) because the anchor-scroll
 * offsets in the details nav have to match it exactly.
 */
export function SiteHeader() {
  return (
    <header className="sticky top-0 z-30 h-header-h border-b border-border-subtle bg-surface">
      {/* T4: logo, search pill, right-hand nav cluster. */}
    </header>
  );
}
